# Generated by pssgen — BelTech Systems LLC
# MIT License — see LICENSE for details
# Questa simulation Makefile for {{ design_name }}

SIM      = vsim
VLOG     = vlog
TOP      = {{ design_name }}_test
WORK     = work

SV_SRCS  = \
{% for f in sv_files %}
    {{ f }} \
{% endfor %}

.PHONY: all compile sim clean

all: compile sim

compile:
	$(VLOG) -sv -work $(WORK) +incdir+. $(SV_SRCS)

sim:
	$(SIM) -c $(WORK).$(TOP) -do "run -all; quit"

clean:
	rm -rf $(WORK) transcript vsim.wlf
