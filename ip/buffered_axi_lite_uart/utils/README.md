Shared utilities for this IP block.
Reserved for Python helpers, constraint generators,
and common scripts used across tool targets.
Currently empty.

Future: edaplayground_package.py — assembles a
submission package for EDA Playground
(edaplayground.com), which supports multiple
simulator backends including Icarus, ModelSim,
Questa, and XSIM.
